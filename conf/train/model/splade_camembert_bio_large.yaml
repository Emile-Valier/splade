# @package _global_

init_dict:
  model_type_or_dir: almanach/camembert-bio-large  # ← identifiant HuggingFace
  model_type_or_dir_q: null
  freeze_d_model: 0
  agg: max
  fp16: true

config:
  tokenizer_type: almanach/camembert-bio-large     # ← même identifiant
  matching_type: splade
